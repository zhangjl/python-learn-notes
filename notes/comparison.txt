2 < 3 < 4 等同于 2 < 3 and 3 < 4
2 < 3 == 3 等同于 2 < 3 and 3 == 3

2 == "2"   # False

is 或者 is not 来验证两个变量是否引用同一个对象
a is b <=> id(a) == id(b)

整数对象和字符串对象是不可变对象, 所以 Python 会很高效的缓存它们。这会造成我们认为 Python 应该创建新对象时,它却没有创建新对象的假象
